<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LogginApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.LogginApp" parent="Base.Theme.LogginApp" />

    <style name="titles">
        <!-- צבע הטקסט -->
        <item name="android:textColor">#FF000000</item>
        <!-- גודל גופן -->
        <item name="android:textSize">24sp</item>
        <!-- סגנון גופן -->
        <item name="android:fontFamily">@font/roberto</item>
        <!-- יישור טקסט -->
        <item name="android:gravity">center</item>
        <!-- ריווח בין האותיות (אופציונלי) -->
        <item name="android:letterSpacing">0.02</item>
        <!-- שולי טקסט -->
        <item name="android:layout_marginBottom">16dp</item>
    </style>

</resources>